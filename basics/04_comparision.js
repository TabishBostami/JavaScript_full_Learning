// common comparisions

// console.log(2>1);
// console.log(2<1);
// console.log(2>=1);
// console.log(2==1);
// console.log(2!=1);
 
//******* some special Compressions ***********

// console.log("2">1);
// {
 
    // Note:- AVOID THIS TYPE OF COMPARISIONS❌     

      // ❌ console.log(null > 0);  //false 
      // ❌ console.log(null == 0); //flase
      // ❌ console.log(null >= 0); //true

      /*The reason is that an equality check == and comparisons > < >= <= work differently.
      Comparisons convert null to a number, treating it as 0.
      That's whv (3) null >= 0 is true and (1) null > 0 is false. */

      // ❌ console.log(undefined > 0);  //false
      // ❌ console.log(undefined == 0); //false
      // ❌ console.log(undefined > 0); //false 
  
      
// }


//Strict check === (it's not check only value it also the data type of value)

// console.log("2" === 2);

//keep hard working.

